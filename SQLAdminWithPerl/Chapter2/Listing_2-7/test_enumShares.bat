@echo off
rem This script doesn't take any command line argument.
rem The server names are hard coded in the script.
rem To enumerate the shares on different servers, change
rem the server names inside the Perl script

perl enumShares.pl
